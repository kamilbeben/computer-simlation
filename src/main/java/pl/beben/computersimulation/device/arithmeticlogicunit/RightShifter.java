package pl.beben.computersimulation.device.arithmeticlogicunit;

import static pl.beben.computersimulation.device.arithmeticlogicunit.Shifter.ShifterType.RIGHT;

public class RightShifter extends Shifter {
  public RightShifter(String id) {
    super(id, RIGHT);
  }

}
